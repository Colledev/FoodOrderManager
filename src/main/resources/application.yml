spring:
  datasource:
    url: jdbc:postgresql://db:5432/food_order_manager
    username: user
    password: password
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  liquibase:
    enabled: true
    change-log: classpath:db/changelog/changelog.xml